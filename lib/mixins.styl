/**
 * Module dependencies
 */

@require './variables'
@require 'shoelace-ui-clockwise'

/**
 * Replace arguments in current-property
 * with `spacing--` variables, if matching.
 * @mixin
 * @opaque
 */

spacing--mixin()
  clockwise--mixin('spacing', arguments)
